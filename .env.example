# RPC endpoint (private/paid RPC recommended for speed/reliability)
PRIVATE_RPC_URL=https://example.solana-mainnet.quiknode.pro/abcdef/

# Private API base URL that returns prebuilt mint transactions (optional)
# If set, the CLI will request a transaction from this API and sign/broadcast it.
# Example expected endpoint: POST {PRIVATE_API_URL}/mint
PRIVATE_API_URL=https://api.your-private-mint-service.example

# Commitment level: processed | confirmed | finalized
COMMITMENT=finalized

# Optional priority fee in microlamports (1 lamport = 1_000_000 microlamports)
PRIORITY_FEE_MICROLAMPORTS=10000

# Optional compute unit limit for the transaction
COMPUTE_UNIT_LIMIT=100000
