Place your Solana wallet keypair JSON files in this directory.
- Supported formats:
  1) solana-keygen format: a JSON array of 64 integers (secret key)
  2) {"secret_key": [..]} dictionary with the same bytes

Example files:
- example1.json
- example2.json
